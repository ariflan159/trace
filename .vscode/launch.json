{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Tau for osu! (Tests, Debug)",
            "type": "coreclr",
            "request": "launch",
            "program": "dotnet",
            "args": [
                "${workspaceRoot}/osu.Game.Rulesets.Trace.Tests/bin/Debug/net8.0/osu.Game.Rulesets.Trace.Tests.dll"
            ],
            "cwd": "${workspaceRoot}",
            "preLaunchTask": "Build tests (Debug)",
            "console": "internalConsole"
        }
    ]
}
